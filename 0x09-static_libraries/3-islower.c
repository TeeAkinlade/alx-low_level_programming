#include "main.h"

/**
 * main - check the code.
 *
 * Return: Always 0.
 */
int _islower(int c)
{
	if ((c >= 'a' && c <= 'z' ))
		_putchar('0');
	else
	{
		_putchar('1');
	_putchar('\n');
	}
	return (0);
}
